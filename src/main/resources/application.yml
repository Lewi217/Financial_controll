spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/financial-control
    username: postgres
    password: 4321
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect
